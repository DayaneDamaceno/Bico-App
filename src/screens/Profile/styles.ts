import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonLogout: {
    backgroundColor: "#5445ff",
    padding: 16,
    alignItems: "center",
  },
  textButtonLogout: { color: "#FFF", fontWeight: "600" },
});
